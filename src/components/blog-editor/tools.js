import Paragraph from "@editorjs/paragraph";
import Header from "@editorjs/header";

const paragraph = {
  class: Paragraph,
  inilineToolbar: true,
};

const header = Header;

const tools = {
  paragraph,
  header
};

export default tools;
